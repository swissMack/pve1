# =============================================================================
# MQTT Control Panel - Environment Configuration
# =============================================================================
# Copy this file to .env and update values for your environment.
# DO NOT commit .env files with actual values to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Host Configuration
# -----------------------------------------------------------------------------
# Set these to your infrastructure hosts - used to build service URLs
PROXMOX_HOST=192.168.1.59
MAC_HOST=localhost

# -----------------------------------------------------------------------------
# Backend Server Configuration
# -----------------------------------------------------------------------------
# Local Express server port
API_PORT=3003

# Backend API URL for frontend
VITE_BACKEND_API_URL=http://localhost:3003/api

# Docker project directory (for container control)
# Auto-detects based on platform if not set
# DOCKER_DIR=/path/to/MQTTServer/docker

# -----------------------------------------------------------------------------
# MQTT Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# MQTT broker URL for backend TCP connection
# REQUIRED: Application will fail to start without this
MQTT_BROKER_URL=mqtt://localhost:1883

# Frontend MQTT WebSocket connection for real-time data
VITE_MQTT_BROKER_URL=ws://localhost:8083/mqtt

# -----------------------------------------------------------------------------
# SIM Card Portal API Integration
# -----------------------------------------------------------------------------
# Portal API URL (for device management, mediation, and consumption analytics)
# Local development:
VITE_PORTAL_API_URL=http://localhost:3001

# UpCloud deployment:
# VITE_PORTAL_API_URL=http://5.22.211.72:3001

# API key for authenticating with Portal API
VITE_PORTAL_API_KEY=your_api_key_here

# Analytics API URL (now uses same API as portal - consumption endpoints)
# The consumption/analytics endpoints are served from the same API server
# Legacy setting - kept for backwards compatibility but not required
# VITE_ANALYTICS_API_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# EMQX Management (REQUIRED)
# -----------------------------------------------------------------------------
# EMQX Dashboard/API URL
# REQUIRED: Application will fail to start without this
EMQX_API_URL=http://localhost:18083

# EMQX Dashboard URL for frontend links
VITE_EMQX_DASHBOARD_URL=http://localhost:18083

# -----------------------------------------------------------------------------
# Monitoring Services
# -----------------------------------------------------------------------------
# Grafana dashboard URL
VITE_GRAFANA_URL=http://localhost:3000

# InfluxDB URL and token
VITE_INFLUXDB_URL=http://localhost:8086
VITE_INFLUXDB_TOKEN=your-influxdb-token

# -----------------------------------------------------------------------------
# Development Settings
# -----------------------------------------------------------------------------
# Vite dev server port
VITE_PORT=5174

# -----------------------------------------------------------------------------
# Docker Deployment Settings
# -----------------------------------------------------------------------------
# Control panel external port (frontend nginx)
CONTROL_PANEL_PORT=5174

# Docker configuration for MQTT network
# When running in Docker, use service names instead of IPs:
# MQTT_BROKER_URL=mqtt://mqtt-emqx:1883
# EMQX_API_URL=http://mqtt-emqx:18083

# Docker directory for container control (mounted volume path)
DOCKER_DIR=/root/MQTTServer/docker

# -----------------------------------------------------------------------------
# UpCloud Deployment Configuration
# -----------------------------------------------------------------------------
# Uncomment and use these settings when connecting to UpCloud deployment
# Server IP: 5.22.211.72
#
# VITE_PORTAL_API_URL=http://5.22.211.72:3001
# VITE_MQTT_BROKER_URL=ws://5.22.211.72:8083/mqtt
# VITE_EMQX_DASHBOARD_URL=http://5.22.211.72:18083
# MQTT_BROKER_URL=mqtt://5.22.211.72:1883
# EMQX_API_URL=http://5.22.211.72:18083
