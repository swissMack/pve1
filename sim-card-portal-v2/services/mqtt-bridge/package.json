{
  "name": "mqtt-bridge",
  "version": "1.0.0",
  "description": "MQTT Bridge Service for SIM Card Portal - receives device telemetry and broadcasts via WebSocket",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.46.1",
    "mqtt": "^5.10.0",
    "ws": "^8.18.0",
    "pg": "^8.13.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
